# Release Input

## Version

0.2.0

## Merged changes

- Added duplicate issue detection.
- Fixed crash in release note generation when a PR has no labels.
- Deprecated the old `--provider` flag in favor of `--agent-command`.

## Known risks

- Provider command presets still need more platform coverage.

